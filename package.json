{
  "name": "payment-hub",
  "version": "1.0.0",
  "description": "project to encapsulate all payment providers in one under the same rules. Let's make payment simple!",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist"
  },
  "keywords": [
    "payment",
    "psp",
    "nodejs"
  ],
  "author": "Daniel Martínez <damarna85@gmail.com> (https://github.com/damarna85)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "braintree": "^2.4.0",
    "stripe": "^5.3.0"
  }
}
